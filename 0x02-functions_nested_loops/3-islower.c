#include "main.h"

/**
 * _islower - check if a character is lower
 * @c: is an ascii character
 * Return: 1 if the character is loxer, 0 otherwise
 */

int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
